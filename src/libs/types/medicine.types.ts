export interface Medicine {
  id: string;
  name: string;
  quantity: number;
  unit: string;
  expiration: string;
  inDate: string;
  outDate: string;
  categoryId: string;
  description: string;
}
